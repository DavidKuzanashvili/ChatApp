<header class="navigation">
    <nav>
        @if (User.Identity.IsAuthenticated)
        {
            <a asp-controller="Account" asp-action="Logout">
                Log Out
            </a>
        }
    </nav>
</header>

